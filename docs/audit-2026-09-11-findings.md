# Deep audit, 2026-09-11 — findings register

A full read-only audit of the tree by seven independent passes (kernels/quant,
format+ModelIO+streaming, HTTP server, forward runner+KV cache, tokenizer+
sampling, repacker+memory+continuity, app+CLI+decode service), followed by
verification and fixes. This file is the register: every finding, where it is,
and what happened to it, so nothing found is lost and a reader can tell a fixed
bug from a known one.

**Status vocabulary.** `fixed` — changed in this audit. `open` — real, verified
by me against the code, not yet changed. `rejected` — reported, then disproved
by reading the mechanism. `deliberate` — real but accepted; the reason is
written down. `unconfirmed` — reported by a pass, mechanism not yet read by me.
Nothing here is asserted on a subagent's word alone; every `fixed` and `open`
row is something I traced in the source myself. That is a claim about
provenance, not infallibility: `Rejected` holds a row I had marked verified and
got wrong, and says how.

---

## Fixed

| # | Sev | Where | What was wrong |
| --- | --- | --- | --- |
| C1 | high | `NVMAIMemory/MemoryTools.swift` | `Int(Double)` traps on NaN, ±infinity and anything outside `Int`'s range, and the value comes straight from a model-authored tool argument (`limit=1e999` parses as +infinity). It aborted the process holding the loaded model. Clamps now. |
| C2 | high | `ContinuityCore/Session/SessionLog.swift` | `pruneTurns(keeping: 0)` computed `turnStarts[count - 0]` — one past the end, a trap. `keeping: 0` is now a cut past the end, which is what "drop every turn" means; session-boundary events survive per the doc comment. |
| C3 | high | `NVMAI/Infrastructure/Streaming/PreadExpertStreamer.swift` | `precondition(experts.count <= slotCount)` trapped while the comment two lines above promised a throw and both callers already handle `nil`. Reachable from `--expert-cache-slots 8` against Qwen3.8-Flash-Next (top-10 routing, prefill tiles up to 16). |
| C4 | medium | `docs/v4.5-ane-prefill.md` | Claimed ANE prefill is "off by default"; `environmentValue` returns `.on` when the variable is unset, and has since v4.6. Marked historical and corrected. |
| C5 | medium | `NVMAI/Runtime/Prefill/ANEPrefillAttention.swift` | The same false claim in the enum's own doc comment ("off by default … never silently selected"). |
| C6 | medium | `NVMAICLI/Args.swift`, `NVMAIServer/Core/ServerArguments.swift` | `--expert-cache-slots` help hardcoded a list that stopped at 128 while the validator accepts 40/48/112/160/192/256. Now spelled from `RuntimeConfiguration.allowedExpertCacheSlots`, so it cannot drift again. |
| C7 | medium-high | `tools/golden-baseline.sh` | Never pinned `NVMAI_PREFILL_ANE`, which defaults to `on`, and the ANE path is deliberately not byte-identical to the GPU path. The only real-inference regression gate followed a default that depends on prompt length and on whether a sidecar happens to be installed. Exports `NVMAI_PREFILL_ANE=off` now. |
| C8 | high | `NVMAI/Kernels/MoE/MoE.swift` | `routerLogits` was allocated at 256 floats while `encodeRouter`'s guard had been raised to 512 "for Qwen3.8-Flash-Next", in a comment calling the old 256 "a conservative guard rather than a width limit". At 512 experts the router GEMV wrote 2048 bytes through a 1024-byte buffer, every layer, every token — non-faulting only because driver allocations are page-granular. Allocation and guard now share `MoE.maxRouterExperts`, with a length precondition. |
| C9 | medium | `NVMAI/Metal/Sampling/logit.metal` | `sample_topk64_final` seeded `picked` from `indices[0]` with no `kept > 0` guard, so a row with no finite mass emitted the `0xFFFFFFFF` sentinel as a token id. The generic `sample` kernel guards exactly this case; the top-64 variant is the default for topK ≤ 64, which is the app's 20. |
| C10 | medium | `NVMAI/Metal/Prefill/prefill.metal` | The prefill router skipped an expert on an exact tie with the k-th score (`s <= top_score[KK-1]`) where decode admits it and applies the lower-index tie-break (`s < …`, with a comment saying so). Identical logits could route differently in a prompt and its continuation — which the kernel's own nearby comment forbids. |
| C11 | high | `ResponsesAPIModels.swift`, `AnthropicModels.swift` | Both mappers filled omitted sampling with the generic `GenerationDefaults` before validation, so the served model's profile was unreachable. The validator resolves `request.value ?? sampling.value`, so Qwen3.8-Flash-Next sampled at 0.6 instead of its card's 1.0 on two of three surfaces. Fields stay nil; each mapper test pins it with a non-house sampling value (the old test could not tell the paths apart, because its `sampling` was already 0.6). |
| C12 | medium | `NVMAI/CPUEngine/AffineSnapshot.swift` | The `.gturbo` reader validated its scale/bias spans only as non-empty, while the safetensors branch beside it checks the exact extent. The resident file is a single mapping, so a wrong span dequantized neighbouring bytes instead of failing. Same check now; the equivalence gate confirms real installs pass. |
| C13 | critical | `NVMAI/Infrastructure/ModelIO/ResidentIndex.swift` | Only `indexSize <= st_size` was checked, and every entry bound the decoder enforces comes from `indexSize + residentSize` in that same header. A same-length edit could declare a huge payload region and point an entry past the mapping — SIGBUS or silent garbage, since the CPU loader hands out pointers with no further check. The region is bounded against the real file now; the test fails with the guard removed, which is how the vulnerability was confirmed. |
| C14 | high | `NVMAIRepack/Core/Remote/RemoteStreamingRepacker.swift` | The disk reservation counted `model_weights.bin` + `packed_experts/**` only, while every passthrough file is ftruncated and written in full. Qwen3.8's ~95 GiB n-gram table (cap 256 GiB) meant a ~168 GiB install passed a ~66 GiB check and died with ENOSPC after tens of GiB. Both sites count them; `--share-ngram-table`'s hardlink case is excluded for free, because that file never enters `plan.passthroughFiles`. |
| C15 | high | `NVMAIMemory/InMemoryStore.swift` | `MemoryRanking.rank` never read `query.limit`. `ContinuityStore` computed `bounded.limit` and passed it in — where it was ignored — so the durable backend returned every match up to its 2000-candidate scan for a query asking for ten, while the in-memory backend honoured the bound. Thousands of up-to-64-KiB records into the model's context, and two backends disagreeing about the same query. |
| C16 | critical | `NVMAI/Runtime/Inference/RealForwardRunner.swift` | `restoreInferenceState` restored KV+GDN and called only `resetTransientState()`, which does not clear the sparse indexer's pooled keys or the PLE convolution window — `reset()` does. A restored prefix then ranked blocks from keys never rebuilt for it, and hashed n-grams from the previous conversation's predecessors: silent wrong output. Now refused with `stateNotInSnapshot`, scoped to the families that enable those subsystems, so the MoE families keep restoring and Qwen3.8 re-prefills. |
| C17 | medium | `NVMAICLI/Args.swift` | `--rdadvise` documented "default off" while the default leaves advice on; `--expert-cache-slots` documented a fixed default of 64 that is really derived from the model profile; `--repetition-penalty` accepted values below 1 that every other front end rejects — and below 1 the kernel *multiplies* the repeated logit, rewarding repetition. |
| C18 | low | `NVMAIApp/Mac/Generation/PromptComposerView.swift` | The prompt tip stated "The default temperature is 0.60" unconditionally; the app follows the model profile, which is 1.0 for Qwen3.8-Flash-Next. |
| C19 | high | `NVMAIBench/main.swift` | `cpu35` accumulated oracle failures, printed "N of 3 wrong", and exited 0 — and this is the command that exists to be scripted as the check that the Swift forward pass matches the oracle. Both missing-tokenizer paths also exited 0. Non-zero now. |
| C20 | high | `NVMAIServer/Core/ServerPromptCache.swift`, `ServerInference.swift` | A mid-session reasoning switch was silently reverted on a *text-continuation* cache hit: `matchTextContinuation` re-renders the tail, and it was handed the session's tokenizer, so the model saw a generation prompt built for the loaded level while the decoder was built for the requested one — chain-of-thought into `content`, or the whole answer reported as reasoning with `content` empty. Comparing rendered token IDs (which the surrounding claim relied on) only covers the direct-prefix path. A level change now invalidates the cache and re-prefills, which is what "a switch is a miss" always meant. |
| C21 | high | `NVMAIServer/Core/HTTPServer.swift:1643`, `:1667` | A streaming request rejected *before* admission (queue full, shutting down) has no SSE head — `startStream` runs on admission — yet `handleAsyncFailure` queued error and `[DONE]` frames that the drainer wrote as body parts. The client got `data: {...}` bytes where a 429 belonged, on all three streaming surfaces. The SSE branch is now gated on `StreamState.isStarted`, and a pre-admission failure falls through to the writers that emit a real status. |
| C22 | high | `NVMAI/Runtime/Inference/RealForwardRunner+Residual.swift:438`, `:467` | The prefill QSA indexer projections called `prefillQMM.encode` unconditionally, while decode branches on `dtype == 1` to a bf16 GEMV. `prepare_qwen38.py` promotes `index_q_proj`/`index_k_proj` to bf16 for an 8-bit build, so prefill read the first k bytes of each 2k-byte row as 8-bit codes and multiplied by scales from offset 0 — and `validateRuntimeSchema` has no indexer check, so the install loaded clean. Both closures now branch on dtype and use per-row bf16 GEMVs, the same shape the hyper-connection prefill projections use. Gated on `dtype == 1`, so the shipped 4-bit path is untouched. |
| C23 | low | `NVMAI/Runtime/Generation/Sampler.swift:89` | `GenerationConfig.validate()` checked temperature, topK, topP and presencePenalty but not `repetitionPenalty` — the one float the sampler divides by. At 0 that computes `z / 0`, and below 1 it *multiplies* the repeated logit, rewarding repetition. Every production entry point checked it separately; a library caller going through `validate` alone did not. |
| C24 | medium | `NVMAIRepack/Core/Format/IndexLoader.swift:58` | Every packed tensor's logical width is derived as `scalesShape.last * 64`, a literal, while `baseGroupSize` is read from the source config and never constrained. A source with any other group size wrote an install whose `shape[1]` was wrong by `64/groupSize` — rejected by the runtime at load, but only after 20–236 GB were written. Refused at the point the config is read. |
| C25 | medium | `NVMAIMemory/SessionJournal.swift:127` | The truncation guard is in UTF-8 bytes but the cut was in Characters, so multibyte text over the byte budget and under the character budget was "summarised" into the whole body plus a duplicated tail with a **negative** omitted count (2000 CJK characters → `[... -3072 bytes omitted ...]`). The cut now walks scalar boundaries and the counts are clamped. |
| C26 | medium | `ContinuityCore/Persistence/Journal.swift:382` | `compact` closed the append descriptor and then `descriptor = try openForAppend(url)`; a throw from the reopen left the field holding the closed number, so the `descriptor >= 0` guard in `writeFully`/`barrier` passed and a later append would target whatever the kernel had since given that number. Cleared before reopening. |
| C27 | medium | `NVMAIRepack/Core/Remote/SourceByteProvider.swift:89` | The destination-fd cache closed a stale descriptor and left the entry in place, so a throw from the reopen meant the function-exit `defer` closed that number a second time — possibly the range temp file or another destination. The entry is removed before the close, matching `LocalSourceByteProvider`; extracting it also keeps `copyBatch` under the function-length gate. |
| C28 | medium | `NVMAIFormat/GTurboManifestV1.swift:200` | Per-tensor quant overrides decoded with `try?` and were never validated: a malformed override was dropped in silence, which is exactly how a width goes missing and the reader falls back to a slot that unpacks the same bytes wrongly. They decode with `try` now, and a declared width outside {4, 8} is refused at load with the key named. |
| C29 | medium | `NVMAI/Infrastructure/Streaming/NgramTableReader.swift:63` | `precondition(rowDim > 0 && rowCount > 0)` trapped on geometry read from `ple_constants.json` — a file on disk, not a fact of the build — and `rowCount &* UInt64(bytes)` wrapped, so a huge row count collapsed `expected` and let the size guard accept a table built for another model. Both are thrown errors now with checked arithmetic; the test aborts the process on the old code. |
| C30 | medium | `NVMAI/Runtime/Family/PLEConstants.swift`, `RealForwardRunner.swift:598` | Nothing compared the sidecar's geometry with the architecture. `headCount * pleHeadDim` is the fp16 values one token gathers while `PLEBlock`'s embedding buffer is sized from `cfg.ple.embedDim`, so a sidecar from another model had the gather write past that buffer — host heap corruption, not a GPU fault — or feed the block wrong-width rows, silently. It is validated before anything derives state from it, which also has to happen before `makeHash()` because `PLEHash`'s own consistency checks are preconditions and would trap on a corrupt file. |
| C31 | medium | `NVMAI/Runtime/Inference/Model.swift:988` | The MoE and GDN kernels stage activations into fixed 2816-element threadgroup tiles (`kMoEXMaxD`, `xt[2816]`) with the staging loop bounded by the configured hidden size, and nothing bounded that size. A wider model writes past the tile into whatever shares the threadgroup memory. Refused at load now; latent, since every preset is 2048/2560/2816. |
| C32 | medium | `ContinuityCore/Session/SessionLog.swift:268` | `turns(taskID:)` folded a whole task with one `pendingPrompt` while `events(taskID:)` groups per session, so an unanswered prompt in session A was consumed by session B's prompt-less reply and filed under **B's** session carrying A's text — mis-attributed content that then fed context assembly and consolidation. Pending prompts are keyed by session now, and the list is sorted by timestamp so "newest last" is a property of the data rather than of the order two loops happened to run in. |
| C33 | low-medium | `NVMAIServer/Core/ManagedModelBackend.swift:103` | `unload()` looped on `while session != nil`, and `session` is nil until a load finishes — so an unload arriving during a load returned false immediately while the load went on to complete and stay resident. The caller was told nothing was released and the memory the endpoint exists to free stayed mapped. It waits the load out now, re-checking rather than trusting one await, because a concurrent awaiter of the same task can resume before `residentSession` assigns `session`. |
| C34 | low | `NVMAI/Kernels/CPU/Int8AffineGEMV.swift`, `CPUExpertFFN.swift`, `CPUEngine/CPUTensorOps.swift` | The CPU affine GEMVs derive two things from the input width `n`: the scale/bias groups per row (`n / 64`) and the packed bytes per row (`n / 2` at 4 bits, `n` at 8). A width that is not a whole number of groups makes those disagree, so every row after the first is read from the wrong byte offset and `x` is truncated to whole groups — silently wrong output, not a short one. Both header and doc comment stated the requirement and nothing enforced it, while every Metal wrapper checks. Guarded at all three entry points now; the suite passes unchanged, which is the evidence no caller relied on the lax behaviour. |
| C35 | low | `NVMAI/Kernels/Attention/Attention.swift:421` | `attention_decode_gqa_swa_partial` sizes its threadgroup arrays to `kAttnMaxQPerKV` (2) and *returns without writing its partials* when the query span is wider, leaving the previous layer's values for the combine pass to normalize. Only `splitGeometry`'s `qPerKV <= 2` clause kept it off that path. Stated at the point the kernel is chosen now, so reaching it another way fails loudly instead of quietly. |
| C36 | low | `NVMAI/Runtime/Generation/Sampler.swift:302` | The incremental repetition-penalty history folded `history.last` per call. That is right for the one production caller, which appends exactly one token between samples, but a speculative path appending two folds one — and because the penalty is applied once per id in the table rather than scaled by frequency, an id appearing only as the *first* of a pair was never penalized at all. It now folds every id appended since the last call, and re-seeds when the caller replaces the history instead of extending it. The test observes the penalty's in-place logit edit directly, so it is deterministic: it fails on the old code by name. |
| C37 | medium | `NVMAIServer/Core/ServerInference.swift:1442` | `count_tokens` promised "the same encoding generation uses, minus the generation" and encoded `request.messages`/`tools` verbatim, while `preparePrompt` applies the CLI strip and then prepends the concise-mode system prompt. The two errors point in opposite directions — inflated for the `<model>-fast` alias and when `NVMAI_STRIP_CLI_PROMPT` is set, under-reported in concise mode — so neither appeared as one suspicious number. It renders through the same two transformations now, in the same order, and resolves the tokenizer for the requested reasoning level the way `generate` does. `concisePrompt` is a property of the loaded session and is passed in; the router's session-less path counts without one, which the comment states. |
| C38 | medium | `NVMAIServer/Core/HTTPServer.swift:1916` (found while verifying C39) | **A flaky crash in the streaming lifecycle, ~1 full-suite run in 4.** `writeHeartbeat` writes a body part gated only on `started && !stopped`, and `stop()` runs in the request task's `defer` — *after* the drainer writes the terminal `.end`. A ping landing in that window is a body with no head outstanding: NIO's `HTTPServerProtocolErrorHandler` preconditions on it (so the whole test run aborts) and in release it is a malformed response. The suite schedules heartbeats every **10 ms**, so the window was hit intermittently; production's 5 s interval only makes it rarer. The drainer stops the heartbeat before writing `.end` now. Found because the crash appeared once while I was verifying C39 and I traced it rather than re-running until it passed; it is independent of C39 — that change only shifted timings. |
| C39 | medium | `NVMAIServer/Core/HTTPServer.swift:2175` (same investigation) | **An empty terminal frame left the drainer parked forever.** `enqueueTerminal` resumed a parked `next()` only by pushing a frame, so `enqueueTerminal([], closeWhenDrained: true)` — which the responses and messages surfaces send on cancellation — set `closed` and left the waiting continuation unresumed. The drainer never finished, so the request task and the in-flight count it decrements stayed outstanding. Also added `abandon()`, used when a request is refused before its SSE head exists (C21's fall-through): the error response is written whole by the ordinary path, so the drainer must neither park nor append a second `end`. |
| C40 | medium | `NVMAI/Tokenization/Tokenizer.swift:542` | The manual ChatML renderer emitted a historical assistant turn's raw content, keeping its whole `<think>` reasoning in the prompt, while the bundled template takes everything after `</think>` as the content and wraps it back in `<think>…</think>` only for a turn after the last real user query (the turn being continued). `applyChatTemplate` is the *only* prompt path — `chatMLChatTemplate` is not a fallback — so every multi-turn prompt here was off the model's own template, and the comment claiming byte-for-byte parity was false. `preserve_thinking`, which the template also honours, has no equivalent here. |
| C41 | low-medium | `NVMAIServer/Core/HTTPServer.swift:9`, `:288-330` | **Two unbounded reads on a loopback server, and a 413 that arrived too late.** NIO caps a single header field at 80 KiB and nothing else — its `HTTPDecoder` exposes no field-count or total-size limit (only `leftOverBytesStrategy` and `informationalResponseStrategy`) — so the server would parse and route a request carrying thousands of headers. And the oversized-body 413 was written at `.end`, meaning the whole body was read and discarded first: a client could make the server consume an arbitrary number of bytes before learning the request was refused. Now a bounded header check refuses at the head with 431, and the 413 is written the moment the cap is crossed. **The rejection drains a bounded remainder before closing** rather than closing immediately: closing mid-upload turns a readable 413 into a connection reset, which is why the first version of this fix (close inline) failed its own test. The close is also ordered after the queued response write — `writeData` uses `eventLoop.execute`, and closing inline in `channelRead` closes *before* that block runs. |
| C42 | medium | `NVMAI/Runtime/KVCache/KVCacheManager.swift:391` | **The disk-tier prompt-cache restore could never work for a long prefix.** `snapshotSegmentLengths` records `min(position, capacity)`, so the lengths are a function of capacity — and a fresh receiver's full-attention layers start at `initialCapacityTokens` (8192). Any snapshot past 8192 therefore produced different lengths on the two sides and was refused as `invalidLayout`, which is exactly the case the disk tier exists for; the feature appeared simply not to work. The restore grows to the snapshot's position before comparing now. **The save/restore path had no test at all**, which is how this survived — two are added, one pinning the round trip and one pinning the growth (with teeth: removing the `reserve` fails it). |
| C43 | low | `NVMAI/Runtime/Inference/Model.swift:988` | A sliding-window layer (mask 0) is a valid `ArchConfig` value and the CPU engine implements it, but the GPU path does not: the gated attention branch runs every non-linear layer as *full* attention with `fullHeadDim`/`numFullKVHeads`/`fullRopeTheta`, and prefill's neox branch uses `fullRopeTheta` unconditionally. Such a layer would attend to the whole context with the wrong row geometry, silently. No shipped preset declares one (they use 1 and 2), so it is refused at load — implementing windowed handling in that branch is the alternative, and the guard is what says which is missing. |
| C44 | medium | `NVMAI/Runtime/Inference/RealForwardRunner+Decode.swift:970`, `:1055` (was O3) | **The non-gated full-attention decode branch called two INT4-only kernels with no width test.** `fusedQKVGEMV` reads q/k/v as packed nibbles and `int4` reads `o_proj` the same way, while the *gated* branch nine lines up (`:695`), the prefill path (`Prefill.swift:523`) and every other dispatch site in this file already branch on `attentionWeightBits`. Reachable for a manifest with `attnOutputGate == false` — the repacker's default — plus an 8-bit attention slot: the install loads clean (`validateFamilyQuantSupport` only constrains the hyper-connection families) and every attention projection is read at half width with scales and biases taken from the tensor's own `scaleOffset`, so the result is wrong rather than missing. Both call sites now take the same branch the gated path takes, through `encodePrimaryGEMV`, which routes 4-bit, 8-bit and a promoted bf16 projection. Latent in-tree: all four shipped profiles set `attnOutputGate: true`, which is why no profile exposed it. **I audited the class, not just the site**: every `int4.encode` / `fusedQKVGEMV.encode` call in `sources/` is now either inside an explicit `weightBits` test, behind the `if let affine … else int4` pattern whose optional is built as `weightBits == 4 ? nil : …` (`:874`, `:901` — `affine` off `attentionWeightBits`, `affineHead` off `lmHeadWeightBits`), or behind a typed dispatcher that selects on the width (`PrefillFinalRowHeadInt4`, `SharedExpertRuntime`). No unguarded int4-only call remains. |
| C45 | low | `NVMAI/Runtime/Generation/RawCompletion.swift:249`, `:357` (was O29) | **`StopReason.toolCalls` was unreachable in both decode loops.** The reason was decided by testing the *stop* token against `tokenizer.toolResponseID`, inside a branch guarded by `stopTokenIDs.contains(token)` — and `stopTokenIDs` is `[<|im_end|>, <|endoftext|>]`, so the test could never be true. `<tool_response>` is a marker the *client* sends back, not something the model emits to end a turn, which is what the dead branch assumed. Three callers read the reason and were therefore dead with it: `ServerPromptCache.matchToolContinuation` (the tool-round prompt-cache reuse — the reason `rawStopReason` exists on the entry at all), the `orphanToolResponse` diagnostic, and the `rawStop` diagnostic string. The turn's own tokens are the signal now: a `ToolCallMarkerCounter` counts `<tool_call>`/`</tool_call>` barrier tokens as they are emitted and reports `.toolCalls` only when the turn *opened* a call and closed every one it opened, so an unterminated call (a truncated turn) and a stray closer both stay `.endOfTurn` and the orphan diagnostic keeps its meaning — "the parse found no call in a turn that produced a well-formed one". The count is exact, not a heuristic: `validateStreamingDecoder` requires each marker to be a literal ByteLevel barrier with exactly that content, so `<tool_call>` cannot reach the output as spelled text. Three loop tests pin it and the positive one **fails by name on the old code**; the two negatives pin that the fix does not over-reach. The OpenAI-facing `finish_reason` was already correct — `ServerInference` derives `tool_calls` from the parsed calls, not from the reason — so this is the cache path and the diagnostics. |
| C46 | low | `Package.swift:98` | `sources/ContinuityCore/README.md` sat inside a target path undeclared, so every re-planned build printed `warning: found 1 file(s) which are unhandled`. Pre-existing and harmless — the file was never compiled, copied into a bundle, or read at runtime — but a warning that fires on every clean plan is noise a reader learns to skip, which is how a real one gets missed. Excluded explicitly, the mechanism the target next door (`NVMAICLICore`) already uses for `Command`. Verified by re-planning: the warning is gone and the build is otherwise warning-free. |
| C47 | medium | `NVMAI/Metal/Sampling/logit.metal:42`, `:140`, `:256`, `Kernels/Sampling/LogitOutput.swift`, `Runtime/Generation/RawCompletion.swift:427` (was O25) | **One NaN logit silently emptied the whole probability row.** `softcap_value` is `softcap * tanh(z / softcap)`, so `tanh(NaN)` is NaN; `max(x, NaN)` keeps the NaN, the running sum went NaN and stayed NaN, `1/NaN` is NaN, and every probability in the row came out NaN. The sampler can only answer an in-range index, so its documented fallback (token 0) was the answer for that position *and for every position after it*, because the generation feeds the token back -- a broken model, a bad prompt and a bad temperature all looked the same. Two changes: `softcap_value` folds NaN to the `-inf` the sum is already built to tolerate (only NaN -- a `+inf` logit is a claim, and `tanh` saturates it to the best token), and a row with no finite logit at all now writes a *defined* all-zero probability row instead of NaN. **The diagnostic is a published row max, not a new sentinel.** The kernels keep their in-range guarantee unchanged -- that invariant exists because the id indexes the vocabulary and the KV, and an earlier intermittent out-of-range id is why it was hardened -- so instead the single-threadgroup front-end writes the row max to a 4-byte shared buffer and the tiled front-end already had `pair[0]`, now shared rather than private. `Sampler.lastRowHadFiniteLogit` reads whichever front-end ran, and `sampleOnce` throws `GeneratorError.degenerateLogitsRow` when it is false. One NaN among finite logits no longer even reaches that: the row is softmaxed over the finite values, which is the distribution a reader expects. Five tests, and reverting the kernel fails three of them by name while reverting the host check turns the fourth into four silent token-0 emissions. |
| C48 | medium | `NVMAI/Metal/Sampling/logit.metal:80`, `:207` (found while fixing C47) | **A `-inf` logit as the first element a thread sees poisoned that thread's running sum, with no NaN needed from the model.** The online-softmax update is `d = d * scale + exp(z - mn)` with `mn = max(m, z)`. The existing guard covers `d * scale` when the running max is still `-inf`, but not the added term: with the first element `-inf`, `mn` is `-inf` too, so `exp(-inf - -inf)` is `exp(NaN)` and the thread's `d` is NaN for the rest of its elements -- `d * 0 + NaN` stays NaN at every later step. `1/NaN` is NaN, `(NaN > 0)` is false, so `inv_d` ended at 0 and the row came out all zeros: the sampler's fallback again, this time from a logit that is merely `-inf` rather than NaN. Reachable whenever the softcap is disabled (`finalLogitSoftcap == 0`, the Qwen 3.6 production setting) and any logit arrives `-inf`; with the softcap on, `tanh` maps `-inf` to the finite `-softcap` and hides it. The added term is now guarded the same way the `d * scale` term was, in both stage-0 loops (single-threadgroup and tiled stage 1). Numerically inert for finite logits, which is why the healthy paths are unchanged. |
| C49 | low-medium | `NVMAIApp/Core/Inference/DecodeServiceInferenceClient.swift:227`, new `NVMAIApp/Core/Inference/DecodeServiceJobSweep.swift` (was O19) | **A force-quit left the decode helper resident, and the next launch added a second one.** The launchd label is built fresh from `getpid()` and a random token (`com.nvmai.decode.<uid>.<pid>.<token>`), `tearDownService` is the only thing that removes a job, and it runs on the way *out* -- so a crash or a force-quit left the job bootstrapped with the model mapped (about 20 GB), and `ensureProcess` only ever asked about its *own* label, so nothing noticed. `launchIndependentService` now sweeps before it bootstraps: it reads `launchctl list`, keeps the labels under our prefix and uid, and removes the ones whose owning pid is gone, through the same `tearDownService` that kills a helper which survives its bootout, and unlinking that job's socket file. The rule is deliberately conservative -- a live owner is left alone, whether that is another running app or a pid the kernel has reused since -- because leaving an orphan in place is recoverable and killing a stranger's helper is not. Five tests pin the parse and the decision (uid filter, malformed labels, socket name matching what the launch path creates, the alive-owner case, pid probing); the launch-and-kill half needs a real launchd and is exercised by running the app, which is stated rather than implied. |
| C50 | medium | `NVMAIMemory/MemoryService.swift:255` (was O31, one of five) | **The workspace cap deleted a workspace another process was inside.** `sweepStaleWorkspaces` computes its candidates as every project file under the storage directory minus the ones *this* process has open, then deletes whatever is past `maximumWorkspaces` by path -- including `<journal>.lock`, which is the file another process's `flock` is attached to. The owner keeps appending to a deleted inode, and its next compaction rewrites that inode into nothing: silent loss of the facts the cap is supposed to be the only rule allowed to remove. `FileJournal` takes `flock(LOCK_EX | LOCK_NB)` on that file, so the guard is the same call, taken and released before the delete; an unreadable file or an unexpected `flock` failure counts as held, because skipping a deletable file costs disk and deleting a live one costs data. The retention pass already had this property (it opens the file through the ordinary engine, which locks it) -- only the cap was deleting by path. Three tests: the locked oldest workspace survives with its lock file while an unlocked one past the cap is still deleted, the cap still deletes with no lock present, and a workspace that was never opened is not treated as held. Removing the guard fails the first by name. |
| C51 | medium | `ContinuityCore/Persistence/Journal.swift:311`, `:334` (was O31, one of five) | **An unreadable journal replayed as an empty one, and the next compaction wrote that emptiness over it.** `replay()` -- the engine's load at start -- and the public `read(contentsOf:)` both went through `FileManager.contents(atPath:)`, which answers `nil` for a file that is absent *and* for one that exists but cannot be read (permissions, I/O error, a directory), discarding the errno on the way. The engine replays before it compacts, so a workspace whose file could not be read came up as a new workspace and `compactJournal` replaced the file with a checkpoint containing nothing: the records were destroyed by the operation that exists to preserve them. `MemoryService.expireSessionLog` walks straight into this -- it opens the journal, prunes every session, and compacts. The two cases are now distinguished by an `open`/`read` loop that keeps the errno: absence is still an empty journal, anything else throws the new `JournalError.readFailed`, and `expireSessionLog` returns false having touched nothing. Three tests: a never-written file still reads as empty; an unreadable journal throws from both entry points and its record is still there afterwards; a reopened engine refuses to `start()` and the task it had written survives. Reverting the read path fails the last two by name. |

## Verification status of the fixes

Unit tests and lint gate every batch (1418 tests in 219 suites, `tools/lint.sh`
clean). Three fixes are verified *against real inference* and three are not, and
the difference matters:

- **Covered by real inference:** C12 (the equivalence gate loaded the 2B and 9B
  dense installs through the changed reader), C13 (a full `--verify-install` of
  the Ornith 4-bit install, plus the test that fails when the guard is removed),
  C1/C2/C3/C9-C11/C17-C19/C21/C45 (unit tests with teeth, each confirmed by
  reverting the fix), and C47/C48 (kernel-level teeth plus an end-to-end loop
  test; both are numerically inert for finite logits, so no healthy path
  changes).
- **Not covered — the golden baseline is blocked.** C8 (MoE router scratch), C16
  (the restore guard), C22 (the indexer's bf16 prefill branch) and C44 (the
  non-gated attention widths) change the
  runtime, and `tools/golden-baseline.sh` is the only check that exercises real
  inference. It refused to start because another checkout's
  `swiftpm-testing-helper` was running, and this audit does not terminate a
  process it did not start. C22 is additionally unreachable without an 8-bit
  Qwen3.8 install, which needs a 360 GB convert.

  **Run `tools/golden-baseline.sh --check ornith-4` and `--check qwen38-4` before
  trusting C8, C16, C22 or C44**, and re-capture only if the diff is a deliberate
  numerics change. C44 cannot be reached by either shipped 4-bit profile at all
  (`attnOutputGate: true` on all four, so the gated branch runs) — it needs an
  8-bit, non-gated manifest, and it is a pure refactor of the 4-bit dispatch, so
  the baseline should be *byte-identical*.

  **The equivalence gate's snapshot side is now 2B-only.** The 4B and 9B
  conversion snapshots (both widths, 21.8 GB) were deleted on 2026-09-11 to
  reclaim disk, so `NVMAI_DENSE_EQUIV_PAIRS` can still be pointed at
  `.build/qwen35-2b-affine-4bit` and `.build/qwen35-2b-affine-8bit` but not at
  4B/9B until `tools/prepare_qwen35.py` is re-run for them. The 2B pair was kept
  precisely because it is also the default input of `tools/qwen35_reference.py`,
  `tools/compare_qwen35_precision.py` and `tools/precision_plan_qwen35.py`. C8 should be numerically inert (it removes an out-of-bounds
  access without changing which values are written or read); C16 only refuses a
  path that previously produced wrong output; C22 is gated on `dtype == 1` and
  cannot be reached by the 4-bit build.

## Open — verified, not yet fixed

| # | Sev | Where | What is wrong |
| --- | --- | --- | --- |
| O5 | medium | `NVMAI/Metal/Fusions/fused.metal:329-331` | **The kernel fix is the work.** `hc_read_phase1_int4` declares two live 10,240-half threadgroup arrays (40,992 B against Apple's 32,768 B limit), so it cannot build and `canFuseRead` is permanently false — the fused read path is dead code today and any measurement of it measured nothing. The gate and `try?` are now documented as such rather than implying availability; the fix is to stage one array and read the second vector from device memory. Unverifiable here without a Qwen3.8 run. |
| O14 | medium | `NVMAIApp/Core/Configuration/AppRuntimeOptions.swift:173`, `NVMAIDecodeService/Entry.swift:144` | **The app and the helper disagree about which options are load-time, and I did not resolve it.** `AppLoadedRuntimeKey` omits `prefillEnabled`, `prefillChunkTokens` and `conciseMode`, while `DecodeRuntimeOptions` is `Equatable` over all its fields and the helper throws "generation runtime options do not match the loaded session" on any difference. So changing Prefill or Concise leaves `hasStaleLoadedRuntime` false, no Reload affordance appears, and the generation fails with a cryptic error while the control does nothing. Adding the three fields to the key fixes that but fails two deliberate tests — `loadedRuntimeKeyTracksOnlyLoadTimeChoices` (asserts prefill enabled/chunk leave the key equal) and `requestTimePrefillChangeDoesNotMarkReadySessionStale` — so the app's intent is that prefill is request-time, and the helper is the side that is wrong. I reverted my change rather than override a design decision: resolving it needs a per-field answer, because `conciseMode` is plainly request-time (a prompt flag, no allocation effect) while `prefillChunkTokens` may size the prefill scratch at load, in which case letting a request raise it without a reload would overrun. **Decide per field**: exclude `conciseMode` from the helper's comparison; for the prefill pair, either show a Reload or confirm the scratch is sized for the maximum allowed chunk. |
| O30 | low | `NVMAIServer/Core/ServerInference.swift:1170`, `ModelRouter.swift:439-442` | The reasoning switch reaches the render, the assistant decoder and now `count_tokens` (C37), but not `runRawCompletion`, which still gets the session's tokenizer — so the doc's claim that "decode … follow[s] the switch" remains half true (the detokenizer and the stop-id check run at the loaded level). Currently harmless because `thinkingMode`/`stopTokenIDs` are mode-independent across a loaded folder. |
| O31 | low | `NVMAIApp/Mac/Generation/...`, `NVMAIMemory/...` | Reported tail, mechanism read but impact bounded. **Three left** (the `flock` item became C50 and the journal-read item C51): a Stop pressed inside the generation-start window can be dropped (`cancel()` writes `cancel(nil)` before `activeGenerationID` is set); one 60 s inter-event timeout covers prefill as well as decode, so a slow prefill chunk gets a healthy helper killed and reloaded; `ContextAssembler.memoryItemIDs` is ranking order while its comment says render order. |

## Rejected — the premise did not survive reading the code

Recorded rather than deleted: a finding that is wrong for an instructive reason
is worth more than one that never existed, and this one was listed as "verified
by me" when it was not. I read the host guard and the kernel's `slice` call and
*assumed* what `slice` does to a tensor's extents instead of reading it.

| # | Where | The claim | Why it is wrong |
| --- | --- | --- | --- |
| O23 | `Metal/TensorCore/tensorops.metal:41`, `Kernels/TensorCore/MPPPrefillInt4QMM.swift:73` | The A tile is never clamped to `M`: the host guards `k % 64` but not `m % 64`, so a chunk length that is not a multiple of 64 reads up to 63 rows past the written activations. | `tensor::slice` **shifts the origin and keeps the parent's extents.** `MPPTensorOpsMatMul2d.h`: `A.slice(0, tgid.y*64)` "has same extents as original tensor A but origin shifted to (0,tgid.y*64) i.e. mA[x,y] == A[x,tgid.y*64+y]". The A operand here is declared `dextents<int32_t,2>(kMPPAffineTileK, M)` — the runtime row count *is* its M extent — and the header's own pattern "will do edge checking for all thread groups against extents of original tensor" is dispatched with `MTLSizeMake((M + 63)/64, …)`, so a partial last row tile is the supported case and reads only rows below `M`. The store is masked on `globalM < M && globalN < N`. The `k % 64` guard exists for the opposite reason — K is the extent the descriptor states *statically*, and [llama.cpp a3027337](https://github.com/ggml-org/llama.cpp/commit/a30273376ef669023334fc20ad02ae4ed8196a65) is what happens when a static extent is trusted over an operand holding less valid data than it claims. There is no `m % 64` guard because M is a runtime extent the operation checks; adding one would refuse shapes the API handles and would cost the MPP path on every final chunk. The behaviour is already pinned by `m: 17` and `m: 33`, whose buffers are sized for exactly `m` rows and which compare against a CPU reference. Both sites now carry a comment stating the asymmetry, `m: 100` was added to the production-shape sweep to cover a partial *second* row tile, and MPP tests plus lint pass. |

## False documentation — found and fixed

All seven were corrected in one batch. Two turned out to be more than
documentation and were fixed as behaviour: D3's missing structural validation
(`--verify-install` could certify a directory the runtime refuses, and would hash
a manifest's own `verified-install.json` entry and then overwrite it) and D7's
dead installer branch, which asserted a refusal the runtime does not implement.
D1 removed a function that selected nothing plus the manifest reads that fed it.


| # | Where | What is false |
| --- | --- | --- |
| D1 | `NVMAICLI/Args.swift:146-148`, `Run.swift:70-78` | `--concise` is called "per-quantization" and the code claims the manifest bit width selects the variant; `ConcisePrompt.prompt(forRoutedExpertBits:)` returns `standard` for every width and the manifest read is pinned to `qwen36_35B_A3B`, so `bits` silently falls back to 4 for every other family. |
| D2 | `NVMAI/Infrastructure/ModelIO/VerifiedInstallReceipt.swift:7-11` | Claims loads verify the receipt binding "and file sizes"; the receipt↔manifest comparison never touches the filesystem, and only `model_weights.bin`, `layout.json` and `packed_experts/*.bin` are ever size-checked. Tokenizer and sidecar files are not. |
| D3 | `NVMAIRepack/Core/Verification/VerifiedInstallTool.swift:50-51` | The comment says duplicate filesystem keys are rejected; that decode does not run `GTurboManifestV1`'s structural checks (duplicate, reserved-name and prefix-collision paths), so `--verify-install` can certify a directory the runtime refuses. |
| D4 | `NVMAIValidation/.../Moe.swift:54` | References an `applyStreamed` sibling that no longer exists. |
| D5 | `NVMAI/Runtime/KVCache/KVCacheManager.swift:25-27` | `KVView.validTokenCount` is documented as an inclusive bound, but every caller passes a count and the kernels iterate `p < seqLen`; the public `keyView(layer:)` default is one *fewer* than the count the decode path deliberately passes. |
| D6 | `NVMAI/Metal/Sampling/logit.metal:107-113` (test comment at `SamplerTests.swift:62-67`) | The test comment claims a NaN row is tanh-clamped and therefore does not exercise `kept == 0`; it does not, and the fallback is `kept == 0`. See O25. |
| D7 | `tools/install_models.sh:279-290` | The `unsupported` branch says 8-bit Qwen3.8-Flash-Next "cannot execute -- the runtime refuses it at load". No catalogue row uses that branch, so it is dead, and no such refusal exists: `validateFamilyQuantSupport` allows 4 *and* 8 bits, and `SlotGEMV` implements both. The message predates SlotGEMV and would mislead anyone who wired the branch up as intended. |

## Deliberate, verified, not changed

- `RuntimePrefillANE`'s asymmetry — an explicit `on` with no sidecar throws, the default degrades quietly — is correct and explained in `wasRequestedExplicitly`. Only the doc comment was wrong (C5).
- `executeExpertCachePlan`'s preconditions validate a plan the type itself constructs, not user input; once `makeExpertCachePlan` returns non-nil they hold by construction.
- `MemoryBackend` excluding memory items from `count_tokens` is documented and deliberate.
- `FusedQKVEpilogue`'s extra no-scale V-norm diverges from the prefill neox path, but it is dead code for shipped presets (`attnOutputGate == true` everywhere) and the live gated path matches both `tools/*_reference.py` and the ANE exporter.
- `int8_affine_gemv.c`'s comment claiming its four-accumulator reduce equals the 4-bit chain cannot be true (different rounding order); doc-only, and each kernel serves its own tensors.
- **The KV quantizer's rounding order (was O22) is a deliberate deferral, not an oversight.** `kv_cache_quantize.metal` derives each code from the *exact* group scale/bias and rounds the stored values to fp16 afterwards, where the repo's own convention (`Quantization.swift:77-90`) rounds to the storage type first and quantizes against the rounded values, so the runtime reproduces the same `q`. The difference is a systematic error worth about 1.5% of one 4-bit step — real, and one line to change. It is not changed because it is a **numerics change**: it alters every quantized-KV result, which invalidates the stored golden baselines, and re-capturing them needs model runs this session could not perform (the golden-baseline guard refuses while another checkout's `swiftpm-testing-helper` is running, and this project does not terminate a process it did not start). Changing it now would leave `--check` reporting mismatches indistinguishable from a regression. Do it as its own deliberate numerics change, with a re-capture.

## Unconfirmed — need their mechanism read before they are trusted

- `moe.metal:1007/1016/1030` index `partial[8]`/`blob[sg_idx]` by raw simdgroup id with no `sg < 8` guard; safe only because k8 is selected exactly when `maxStreamedExperts == 8`.
- `dequant_int4.metal:389/462/503` do 4-byte loads on 2-byte-aligned addresses, which the same file calls undefined; no Swift call site dispatches them.
- `kv_cache_quantize.metal:29` derives its element index from `thread_position.x` while the dispatch width is a hard-coded 64.
- `CPUExpertFFN.Offsets` hardcodes 4-bit packing with no width check; only tests call it.
- `VerifiedInstallTool.payloadMaxBytes` (128 GiB) is below the repacker's passthrough cap for `ngram_table.bin` (256 GiB), so an install the repacker accepts can be unverifiable.
- `RemoteStreamingRepacker.swift:298-308` wipes the partial and preallocates inside the `dryRunSpaceCheck` branch when a resume is in flight (no caller sets the flag today).
- `encodeLayout` takes the first non-empty layer's `expertStride` as global without checking uniformity, which `GTurboV1StructuralValidator` requires.
- Unchecked `UInt64` offset arithmetic in `PreadExpertStreamer.swift:417/613/634/1094/1097` and `ExpertStreamer.swift:60-61`; no reachable trigger found.
- `PreadExpertStreamer.readFull` does not retry `EINTR` where every other pread loop in the module does.
- `SSEOutbox.next()` installs its continuation after the cancellation handler, so a drainer cancelled between iterations can park forever; a terminal frame always rescues it today.
- `errorCaught`/`failStream` cancel `activeTask` unconditionally, so with pipelining an I/O error belonging to response N can cancel request N+1.
- `ResponseStore.put` does not move a re-put id to the back of `order`, contrary to its comment.
- `HEAD` to any path other than `/health` and `/v1/models` returns a body.
- `GTurboEncoders.swift:29-30` uses a `precondition` rather than a thrown error for a tensor name over 65535 bytes.
- `NgramTableReader`/`ResidentIndex` accept `shape` values unrelated to `sizeBytes`/`dtype`; a consumer reduces them with trapping `Int` multiplication.
- `NVMAIApp/Core/Configuration/MacAppSettings.swift:11-46` exposes RDADVISE, prefill chunk, expert-cache policy and verification in the Inspector but persists none of them, so they revert silently on a model switch or relaunch.
